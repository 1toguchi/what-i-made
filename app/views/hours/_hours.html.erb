<table class="attr", border="5" rules="all">
<% @user.hours.each do |hour| %>
  <tr>
    <th>勉強内容</th>
    <th>日付</th>
    <th>勉強時間</th>
    
  </tr>
  <tr> 
    <td><%= hour.done %></td>
    <td><%= hour.post_date %></td>
    <td><%= hour.hours %></td>
  </tr>

  <% end %>
</table>